# Exercise the distributed Node bundle weekly and before releases.
# PRs opt in through the needs-ci-full label; normal CI tests Bun directly.

name: CI (full matrix)

on:
  schedule:
    - cron: '15 4 * * 1'
  workflow_dispatch:
  pull_request:
    types: [labeled, synchronize]

permissions:
  contents: read

concurrency:
  group: ci-full-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (node ${{ matrix.node }})
    # PR runs only fire when the `needs-ci-full` label is present.
    # Schedule and workflow_dispatch always run.
    if: >-
      github.event_name != 'pull_request' ||
      contains(github.event.pull_request.labels.*.name, 'needs-ci-full') ||
      (github.event.action == 'labeled' && github.event.label.name == 'needs-ci-full')
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [18, 22]

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: recursive

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ matrix.node }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build CLI bundle
        run: bun run build:bundle

      - name: Verify bundled CLI on Node
        run: |
          node bin/cue.mjs --version
          node bin/cue.mjs list --json > profiles.json
          node -e 'const profiles = require("./profiles.json"); if (!Array.isArray(profiles) || !profiles.some(p => p.name === "core" && p.skillCount > 0)) process.exit(1)'
          node bin/cue.mjs validate core
